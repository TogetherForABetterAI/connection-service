services:
  auth-gateway:
    container_name: auth-gateway
    build: .
    env_file: .env
    networks:
      - tpp-network
    ports:
      - "${PORT}:${PORT}"
    environment:
      - ENVIRONMENT=${ENVIRONMENT}
    volumes:
      - .:/app

networks:
  tpp-network:
    external: true
